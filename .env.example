# Copy to .env (git-ignored) and set real values. Never commit .env.
# Used by the application (local profile) and by docker-compose.yml.
DB_HOST=localhost
DB_PORT=3307
DB_NAME=solgases
DB_USERNAME=solgases
DB_PASSWORD=
DB_ROOT_PASSWORD=
